# '{{ansible_managed}}'

server {
    listen              443 ssl http2;
    server_name         wifir.{{ domain }};
    root                {{ wifir_webroot }};
    include             snippets/ssl;
    index               index.html;
    try_files   $uri    /index.html;

    location ~ /\.git {
        return 404;
    }
}