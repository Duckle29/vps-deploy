server {
    listen              443 ssl http2;
    server_name         {{ domain }};
    root                {{ mikkelcc_webroot }};
    index               index.html;
    try_files   $uri    /index.html;

    include             snippets/ssl;
}